# CertRenova Probe target list.
#
# One target per line. Lines starting with # are ignored.
#
# The service is inferred from the port, so a bare "host:587" does the SMTP
# STARTTLS negotiation without you having to ask for it. Use a scheme when the
# port is non-standard, or when you want to be explicit.
#
# Copy this file to hosts.txt and edit it for your own environment.

# --- Web ---
example.co.jp
www.example.co.jp
portal.example.co.jp:8443

# --- Mail: the certificates nobody watches ---
# These fail silently. No browser warning -- mail just stops being delivered.
smtp://smtp.example.co.jp:587      # SMTP submission, STARTTLS
mail.example.co.jp:25              # SMTP relay, STARTTLS (inferred from the port)
smtps://smtp.example.co.jp         # implicit TLS on 465
imaps://imap.example.co.jp         # implicit TLS on 993
imap://imap.example.co.jp:143      # STARTTLS
pop3s://pop.example.co.jp          # implicit TLS on 995

# --- Directory ---
ldaps://ldap.example.co.jp         # implicit TLS on 636

# --- Databases: expire and the app stops connecting, silently ---
postgres://db.example.co.jp        # PostgreSQL SSLRequest on 5432
db.example.co.jp:3306              # MySQL/MariaDB (inferred from the port)

# --- Public test endpoints (useful for a first run) ---
badssl.com
expired.badssl.com
self-signed.badssl.com
