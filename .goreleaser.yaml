# GoReleaser v2. Validate locally with: goreleaser check
# Builds one static binary and packages it for RHEL (.rpm), Ubuntu/Debian
# (.deb), and Alpine (.apk) in a single run.
version: 2
project_name: certrenova-probe

before:
  hooks:
    - go mod tidy

builds:
  - id: certrenova-probe
    main: .
    binary: certrenova-probe
    env:
      - CGO_ENABLED=0
    goos: [linux]
    goarch: [amd64, arm64]
    ldflags:
      - -s -w -X main.version={{ .Version }}

archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

nfpms:
  - package_name: certrenova-probe
    formats: [deb, rpm, apk]
    maintainer: "Toinet <302558825+toinet-lab@users.noreply.github.com>"
    description: "Read-only TLS certificate inventory tool."
    homepage: "https://github.com/toinet-lab/certrenova-probe"
    license: MIT
    bindir: /usr/bin
    file_name_template: "{{ .ConventionalFileName }}"

checksum:
  name_template: "checksums.txt"

changelog:
  use: github
  sort: asc

release:
  github:
    owner: toinet-lab
    name: certrenova-probe
